general:
  artifacts:
      - "build/test_images/"
      - "build/test_images_diff/"

machine:
  node:
    version: 6.1.0
  timezone:
    America/Anchorage
  services:
    - docker

dependencies:
  override:
    - npm install
    - npm prune && npm ls
    - npm run pretest
    - lsb_release -a
    - google-chrome --version

test:
  override:
    - npm run citest-jasmine
